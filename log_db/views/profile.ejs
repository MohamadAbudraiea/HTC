<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= user.name %>'s Profile</title>
  <style>
    :root {
      --primary: #4a90e2;
      --secondary: #357abd;
      --accent: #6bb1f7;
      --light: #f8f9fb;
      --dark: #2c3e50;
      --gradient: linear-gradient(135deg, #4a90e2 0%, #6bb1f7 100%);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
      background: #f9fbfd;
      color: var(--dark);
      line-height: 1.6;
      min-height: 100vh;
    }

    .container {
      max-width: 1000px;
      margin: 2rem auto;
      padding: 0 1.5rem;
    }

    .profile-card {
      background: white;
      border-radius: 1.5rem;
      padding: 2.5rem;
      box-shadow: 0 10px 30px rgba(0,0,0,0.08);
      position: relative;
      overflow: hidden;
    }

    .profile-header {
      text-align: center;
      margin-bottom: 2.5rem;
    }

    .profile-pic {
      width: 160px;
      height: 160px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 1.5rem;
      border: 4px solid white;
      box-shadow: 0 10px 30px rgba(74,144,226,0.2);
    }

    .profile-details {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1.5rem;
    }

    .detail-card {
      background: var(--light);
      border-radius: 1rem;
      padding: 1.5rem;
      transition: transform 0.3s ease;
      border-left: 4px solid var(--primary);
      position: relative;
    }

    .detail-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 5px 15px rgba(74,144,226,0.1);
    }

    .detail-header {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 1rem;
    }

    .detail-icon {
      width: 32px;
      height: 32px;
      padding: 6px;
      background: var(--primary);
      border-radius: 8px;
      color: white;
    }

    /* Navigation Styles */
    .navbar {
      background: var(--gradient);
      padding: 0.8rem 2rem;
      box-shadow: 0 4px 20px rgba(74,144,226,0.15);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }

    .nav-links a {
      color: white;
      text-decoration: none;
      font-weight: 500;
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      transition: background 0.3s ease;
    }

    .nav-links a:hover {
      background: rgba(255,255,255,0.15);
    }

    .logo {
      height: 40px;
    }

    /* Notification Styles */
    .notification-icon {
      color: white;
      position: relative;
      margin-left: 2rem;
    }

    .notification-badge {
      position: absolute;
      top: -5px;
      right: -10px;
      background: #ff4757;
      color: white;
      padding: 2px 6px;
      border-radius: 10px;
      font-size: 0.8rem;
    }

    /* Footer Styles */
    footer {
      background: var(--dark);
      color: var(--light);
      margin-top: 3rem;
      padding: 2rem;
      text-align: center;
      border-radius: 1rem 1rem 0 0;
    }

    @media (max-width: 768px) {
      .nav-links {
        display: none;
      }
      
      .profile-card {
        padding: 1.5rem;
      }
      
      .profile-pic {
        width: 120px;
        height: 120px;
      }
    }

    .status-indicator {
      position: absolute;
      top: 1rem;
      right: 1rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.9rem;
      color: var(--primary);
    }

    .status-dot {
      width: 10px;
      height: 10px;
      background: #2ecc71;
      border-radius: 50%;
      animation: pulse 1.5s infinite;
    }

    @keyframes pulse {
      0% { box-shadow: 0 0 0 0 rgba(46,204,113,0.4); }
      70% { box-shadow: 0 0 0 10px rgba(46,204,113,0); }
      100% { box-shadow: 0 0 0 0 rgba(46,204,113,0); }
    }
  </style>
</head>
<body>
  <!-- Navigation Bar -->
  <nav class="navbar">
    <img src="https://i.pinimg.com/736x/33/37/ee/3337eeff3be1747f984d9d8e59eeb1c1.jpg" alt="Logo" class="logo">
    <ul class="nav-links">
      <li><a href="/home">Home</a></li>
      <li><a href="/companies">Companies</a></li>
      <li><a href="/certi-0">Certificates</a></li>
      <li><a href="/courses">Courses</a></li>
      <li><a href="/profile" class="active">Profile</a></li>
    </ul>
    <div class="notification-icon">
      <i class="fas fa-bell"></i>
      <span class="notification-badge">2</span>
    </div>
  </nav>

  <div class="container">
    <div class="profile-card">
      <div class="profile-header">
        <img src="https://i.pinimg.com/736x/f8/9e/7e/f89e7e8f14bc002a9b0f9f453bec8805.jpg" 
             alt="Profile Picture" 
             class="profile-pic">
        <h1 style="color: var(--primary); margin-bottom: 0.5rem;"><%= user.name %></h1>
        <div class="status-indicator">
          <div class="status-dot"></div>
          <span>Active Now</span>
        </div>
        <p style="color: var(--dark);">Student ID: <%= user.std_id %></p>
      </div>

      <div class="profile-details">
        <div class="detail-card">
          <div class="detail-header">
            <svg class="detail-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
            <div>
              <h3>Contact Information</h3>
              <p><%= user.email %></p>
            </div>
          </div>
        </div>

        <div class="detail-card">
          <div class="detail-header">
            <svg class="detail-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
            </svg>
            <div>
              <h3>University</h3>
              <p><%= user.university %></p>
            </div>
          </div>
        </div>

        <div class="detail-card">
          <div class="detail-header">
            <svg class="detail-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>
            </svg>
            <div>
              <h3>Academic Major</h3>
              <p><%= user.major %></p>
            </div>
          </div>
        </div>

        <div class="detail-card">
          <div class="detail-header">
            <svg class="detail-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
            </svg>
            <div>
              <h3>Academic Performance</h3>
              <p>GPA: <%= user.gpa %></p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <footer>
    <p>&copy; 2025 Training Hub. Empowering Future Professionals.</p>
  </footer>

  <script>
    // Mobile menu toggle
    function toggleMenu() {
      const navLinks = document.querySelector('.nav-links');
      navLinks.style.display = navLinks.style.display === 'flex' ? 'none' : 'flex';
    }

    // Notification handling
    function toggleNotifications() {
      const dropdown = document.querySelector('.notification-dropdown');
      dropdown.classList.toggle('active');
    }

    // Dynamic status update
    function updateStatus() {
      const statusDot = document.querySelector('.status-dot');
      statusDot.style.backgroundColor = navigator.onLine ? '#2ecc71' : '#e74c3c';
    }

    // Update status on load and network changes
    window.addEventListener('load', updateStatus);
    window.addEventListener('online', updateStatus);
    window.addEventListener('offline', updateStatus);
  </script>
</body>
</html>